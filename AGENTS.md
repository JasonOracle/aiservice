# AI 客服 MVP - 最高执行准则 (AGENTS.md)

## 0. 【核心强制指令】AI 唤醒与进度对齐规约 (AI Wake-up & Alignment)
> **致所有的 AI Agent (Cursor / OpenCode / CodeBuddy 等)**：
> 当你进入本仓库开始编码或对话时，必须**绝对遵循**以下首要规则，这是你“醒来”后要做的第一件事：
> 1. **立刻读取进度条**：强制去阅读 `docs/progress.md` 和 `docs/product.md`。那是本项目的活体进度追踪器和需求来源。
> 2. **你可以做什么 (What you CAN do)**：通过查看 `progress.md`，定位当前停留在哪一个 Phase，并严格按照该 Phase 规定的步骤、库选型和测试标准 (DoD) 进行开发。完成某个阶段后，你必须主动把该步骤勾选为 `[x]`。
> 3. **你不可以做什么 (What you CANNOT do)**：绝对禁止跳步开发！绝对禁止为了应付而写 `// TODO` 假代码！绝对禁止引入文档中未指定的重量级第三方库！

作为本项目的最高指导原则，所有的 AI 智能体在进行代码生成、重构和分析时，还必须遵循以下架构规则：

## 1. 核心技术栈强制约束
- **前端包管理器**：**强制优先使用 `pnpm`**，其次才是 `npm`。严禁混合使用导致 lock 冲突。
- **前端框架**：`Next.js` (App Router) + React。分为 `tob` 和 `toc` 两个独立应用。
- **前端样式**：全面拥抱 Ant Design 生态体系。C端 (`toc`) 的 PC 视图使用 `antd`，移动端视图使用 `antd-mobile`；B端 (`tob`) 采用 `Ant Design X` (包含 `antd`) 以快速搭建具有原生 AI 感的组件。不再使用 `shadcn/ui`。
- **后端与 AI 引擎**：**`Python` + `FastAPI`**。所有的 RAG 检索、Mem0 记忆处理和 AI 流式输出必须在 Python 端实现，以充分展示底层 AI 能力。
- **数据库**：`TiDB Serverless`。Python 端通过 SQLAlchemy / PyMySQL 连接，进行关系型和向量 (Vector) 的存储与检索。

## 2. 视觉与体验第一 (Aesthetics First)
- 界面必须呈现出高级 SaaS 的质感。
- 必须应用现代设计元素：深浅色模式适配、毛玻璃效果 (backdrop-blur)、优雅的字体排版以及微动画 (平滑过渡、打字效果)。
- 所有的页面和组件必须是**响应式**的，能通过 Tailwind 的断点 (`sm:`, `md:`, `lg:`) 在 PC 端和移动端完美展示。

## 3. 代码质量与极简原则
- **拒绝过度设计**：本专案是 MVP 版本，优先保证核心链路（聊天、RAG、扫码接力）跑通，不要编写为“未来可能用到”而准备的冗余代码。
- **类型安全**：全面使用 TypeScript。核心接口必须定义清晰的 `interface` 或 `type`。
- **文档驱动**：所有核心组件、API Route，必须在同级或顶部保留清晰的中文注释，说明其目的、参数和返回值。

## 4. 功能实现高压线
- 所有的 AI 回答必须有 Fallback 机制：如果检索不到相关的知识库内容，模型必须明确告知无法回答并提示转交人工，严禁产生幻觉编造答案。
## 5. 代码变更日志规范 (File Header & Change Log)

任何时候只要**修改已有文件**（包括代码、配置等），必须在文件的**最顶部**严格按以下格式添加或更新中文注释日志（不破坏已有日志流，追加在最上方）。

**【绝对红线 - 代码注释版本号禁令】**
- 5.1. **严禁 AI 擅自在代码变更日志中发明、猜测或递增任何版本号**（例如禁止出现 v1.4, v2.0, v1.x 等字眼）。
- 5.2. 在没有用户明确指定当前版本号的情况下，日志内容**只允许客观描述动作**，绝不允许带任何版本前缀。
*(⚠️ 特例免责：此禁令仅针对代码文件内部。规定的文档重命名备份操作不受此限。)*

✅ 正确格式示例：
```javascript
/**
 * [变更日志]
 * 修改时间：2026-XX-XX
 * AI模型：Gemini 底层
 * 修改内容：[1. 精简重构了某功能; 2. 修复了某边界Bug]
 */
```

❌ 错误格式示例（严禁）：
```javascript
* 修改内容：[v1.5: 修复了某边界Bug]
```

---
##  6. 彻底防滞后：沟通即文档 (Anti-Lag & Zero-Context-Loss)
**场景：在与用户的日常开发沟通中，经常会产生新的临时决策（如：UI 间距调整、某个组件废弃、某个字段改名）。**

- **禁止依赖会话记忆**：AI 严禁将用户口头确认的新需求或变更仅保留在当前会话上下文（Context Window）中。
- **强制落地规则**：一旦代码修改落地并被用户确认，AI 必须**立即、主动**将这些变更同步更新到 `progress.md`（作为进度或备忘）和 `technical_implementation.md`（如果涉及架构/公共组件变动）中。

---


## 7. 演示种子用户 (Seed Data)
项目初始化时，数据库将预装以下 10 个自带人设的测试账号，用于在 C 端登录并展示 Mem0 的“千人千面”记忆能力（统一密码为 `123456`）：

| 手机号 (账号) | 姓名 | 角色画像 | 核心偏好/特征 (用于 Mem0 冷启动) |
| :--- | :--- | :--- | :--- |
| `13800000001` | 张伟 | 资深程序员 | 30岁，极简主义，偏好高性价比数码产品，讨厌啰嗦的推销。 |
| `13800000002` | 李娜 | 美妆达人 | 26岁，关注抗老和防晒，喜欢大牌美妆平替，对包装颜值要求高。 |
| `13800000003` | 王强 | 健身教练 | 32岁，重度增肌期，常买蛋白粉和低脂零食，注重营养成分配料表。 |
| `13800000004` | 赵敏 | 新手宝妈 | 29岁，宝宝6个月大，对母婴产品的安全性极度敏感，容易焦虑。 |
| `13800000005` | 陈杰 | 数码发烧友 | 22岁，大学生，对电脑外设和游戏显卡了如指掌，预算有限但追求极致性能。 |
| `13800000006` | 杨洋 | 职场新人 | 24岁，刚入职场，喜欢购买平价职业装和咖啡豆，工作压力较大。 |
| `13800000007` | 周游 | 户外博主 | 28岁，热爱自驾和露营，经常购买冲锋衣和野外生存装备，对防水性能要求极高。 |
| `13800000008` | 吴婷 | 资深铲屎官 | 27岁，家里养了两只布偶猫，每月在宠物罐头和猫砂上开销很大。 |
| `13800000009` | 孙斌 | 品茶大叔 | 45岁，企业中层，热衷于收集紫砂壶和各类岩茶，追求生活品质，预算充足。 |
| `13800000010` | 林青 | 二次元画师 | 20岁，经常熬夜画图，喜欢收集手办和购买保健品（护肝/护眼），二次元浓度高。 |
